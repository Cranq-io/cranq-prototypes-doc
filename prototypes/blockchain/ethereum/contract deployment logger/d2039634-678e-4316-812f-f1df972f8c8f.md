# Contract deployment logger

_[blockchain/ethereum/Contract deployment logger]_

---

Logs contract deployment events.<br>

---

### Input ports

* __events__: 
    ```
    {
      "field": (
        "transaction hash"
        or "receipt"
        or "confirmation"
        or "contract"
        or "error"
      ),
      "data": any
    }
    ```

